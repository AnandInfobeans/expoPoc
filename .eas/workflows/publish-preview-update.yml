name: Publish preview update

on:
  push:
    branches: ["updates",  "develop_new"]

jobs:
  publish_preview_update:
    name: Publish production update
    type: update
    params:
      branch: production
      platform: android
