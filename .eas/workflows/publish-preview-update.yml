name: Publish preview update

on:
  push:
    branches: ["updates"]

jobs:
  publish_preview_update:
    name: Publish preview update
    type: update
    params:
      branch: updates
      platform: android
