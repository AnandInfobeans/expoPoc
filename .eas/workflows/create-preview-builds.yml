name: Create preview build

on:
  push:
    branches: ["build"]

jobs:
  android_production_build:
    name: Build Android
    type: build
    params:
      platform: android
      profile: production
